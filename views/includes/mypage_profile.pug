link(rel="stylesheet" href="/css/mypage/profile.css")


form.setinline(method="post" enctype="multipart/form-data")
    img.profile.img(src=(`/uploads/${user.profile_image}`) || "" onerror="javascript:this.src='../../images/profile.png'") 
    form(method="POST", enctype="multipart/form-data" )
        input.emailid(name="username" value=`이메일 ID : ${user.username}` readonly)
        input.nickname(value=`닉네임 : ${user.nickname}` readonly)
        div.profilebutton        
            input(type="file", name="b_upfile", accept="image/*")#b_upfile
        button.profilebutton(type="submit" value="image") 프로필이미지 변경
    .myGenre 나의 선호장르 
        each genre in userGenre  
            div.genre= `${genre.GG_genre.genre_name}` || 'null'
    .profilebutton.myPost 내가 쓴 글 보러가기
    .profilebutton.myReply 내가 쓴 댓글 보러가기
    .modal.post
        .head
            h2 나의 댓글
            span.close.post x
        .table
            table
                thead.head
                    tr  
                        th 게시판 
                        th 제목
                        th 작성일시
                tbody.body
                 each post in Reply 
                    tr.result 
                        td= post.board_code
                        td= post.r_content 
                        td= post.r_update_date 
    .modal.reply
        .head
            h2 나의 댓글
            span.close.reply x
        .table
            table
                thead.head
                    tr  
                        th 게시판 
                        th 제목
                        th 작성일시
                tbody.body
                 each post in Reply 
                    tr.result 
                        td= post.board_code
                        td= post.r_content 
                        td= post.r_update_date 


